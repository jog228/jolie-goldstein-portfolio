# Required — your GitHub username (public API, no token needed for basic use)
GITHUB_USERNAME=jog228

# Optional — increases GitHub API rate limit from 60 to 5,000 req/hr
# Generate at: https://github.com/settings/tokens (classic, no scopes needed for public data)
GITHUB_TOKEN=
